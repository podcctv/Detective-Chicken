services:
  api:
    build: .
    restart: unless-stopped
    environment:
      DETECTIVE_CHICKEN_ADDR: ":8080"
    expose:
      - "8080"
    healthcheck:
      test: ["CMD", "/usr/local/bin/detective-chicken-server", "-healthcheck"]
      interval: 30s
      timeout: 3s
      retries: 3

  web:
    build: ./web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "8088:80"

  postgres:
    image: timescale/timescaledb:2.29.0-pg18
    profiles: ["production-data"]
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-detective_chicken}
      POSTGRES_USER: ${POSTGRES_USER:-detective_chicken}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me-before-production}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-detective_chicken} -d ${POSTGRES_DB:-detective_chicken}"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
